# 训练配置
batch_size: 32
learning_rate: 0.001
epochs: 100
data_path: "data/processed"
save_path: "experiments/results/model.pth"

# 模型配置
model:
  in_channels: 3
  out_channels: 3
  
# 数据配置
data:
  image_size: 256
  train_val_split: 0.8 